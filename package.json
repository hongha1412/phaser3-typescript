{
  "name": "phaser3-typescript",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "ts-run index.ts",
    "watch": "ts-run index.ts --watch"
  },
  "dependencies": {
    "detect-collisions": "^9.2.3",
    "phaser": "3.60.0"
  },
  "devDependencies": {
    "phaser3spectorjs": "^0.0.8",
    "typescript-run": "^0.1.1"
  }
}
